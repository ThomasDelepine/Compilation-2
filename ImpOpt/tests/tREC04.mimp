function main() {
  var i;

  i = 0;
  while ((i < 7)) {
    putchar((f1(0, i)(+65)));
    i = (i(+1));
  }
}

function f1(n, max) {
  var tmp;

  tmp = 1;
  if ((n < max)) {
    return((f2((n(+1)), max)(+1)));
  } else {
    return(0);
  }
}

function f2(n, max) {
  var tmp;
  var tmp2;

  tmp = 3;
  tmp2 = 2;
  if ((n < max)) {
    return((f1((n(+1)), max)(+1)));
  } else {
    return(0);
  }
}

